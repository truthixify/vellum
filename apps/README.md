# apps

Runnable applications. Each subfolder is its own Bun/Node package with its own build and deploy story, discovered by the root workspace.

The main dashboard currently lives at the repo root under `dashboard/`, outside this folder. When it's moved into the workspace, this is where it lands: `apps/dashboard/`. No changes required here first.

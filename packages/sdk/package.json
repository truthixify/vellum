{
  "name": "@vellum/sdk",
  "version": "0.1.0",
  "description": "Typed Claim Cell APIs for Vellum and CKB applications.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "CHANGELOG.md",
    "README.md"
  ],
  "sideEffects": false,
  "homepage": "https://github.com/truthixify/vellum#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/truthixify/vellum.git",
    "directory": "packages/sdk"
  },
  "bugs": {
    "url": "https://github.com/truthixify/vellum/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "bun run clean && bun build ./src/index.ts --outdir ./dist --entry-naming index.js --target=browser --format=esm --packages=external --sourcemap=external && tsc -p tsconfig.build.json",
    "clean": "rm -rf ./dist",
    "example": "bun run examples/api.ts",
    "prepack": "bun run build",
    "test": "bun test src",
    "test:testnet": "bun test testnet",
    "typecheck": "tsc --noEmit",
    "verify:package": "bun run build && tsc --noEmit -p tsconfig.examples.json && bun run example && bun pm pack --dry-run --ignore-scripts"
  },
  "dependencies": {
    "@ckb-ccc/core": "1.19.1",
    "@ckb-ccc/did-ckb": "0.2.9",
    "@ipld/dag-cbor": "10.0.2"
  },
  "devDependencies": {
    "bun-types": "1.4.2",
    "typescript": "5.8.3"
  }
}

[package]
name = "vellum-claim-types"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
molecule = { version = "=0.9.2", default-features = false }

[build-dependencies]
molecule-codegen = { version = "=0.9.2", features = ["compiler-plugin"] }
